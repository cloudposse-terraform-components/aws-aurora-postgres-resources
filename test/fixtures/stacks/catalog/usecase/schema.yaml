components:
  terraform:
    aurora-postgres-resources/schema:
      metadata:
        component: aurora-postgres-resources
      vars:
        enabled: true
        additional_schemas:
          test-schema:
            database: postgres
