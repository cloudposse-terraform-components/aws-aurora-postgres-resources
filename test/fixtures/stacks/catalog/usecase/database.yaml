components:
  terraform:
    aurora-postgres-resources/database:
      metadata:
        component: aurora-postgres-resources
      vars:
        enabled: true
        additional_databases: ["test-db"]
